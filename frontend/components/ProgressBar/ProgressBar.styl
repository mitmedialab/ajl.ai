@import '../shared/mixins.styl';

.label {
  visually-hidden();
}

.barContainer {
  very-rounded();
  overflow: hidden;
  height: 10px;
  width: 100%;
  background: #D3E1E8;
  display: inline-block;
}

.bar {
  height: 10px;
  background: $aqua;
}

.inProgress {
  transition: width 200ms;
}
